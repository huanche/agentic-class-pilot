<Slide style={{ width: '1280px', height: '720px', padding: 0, background: '#FFFFFF' }}>
  <Box style={{ position: 'absolute', top: 36, left: 64, right: 64, flexDirection: 'row', gap: 16 }}>
    <Box style={{ width: 8, height: 40, background: '#1B4F9C', borderRadius: 2, marginTop: 4 }} />
    <Box>
      <Text style={{ fontSize: 34, fontWeight: 'bold', color: '#0F2E5C' }}>空壳也能跑：降级链与启动校验</Text>
      <Text style={{ fontSize: 15, color: '#64748B', marginTop: 6 }}>「目前能不能上课」是确定性判断，不依赖模型</Text>
    </Box>
  </Box>
  <Box style={{ position: 'absolute', top: 128, left: 64, right: 64, bottom: 56, gap: 16 }}>
    <Box style={{ background: 'linear-gradient(135deg, #1B4F9C, #0F2E5C)', borderRadius: 12, padding: '20px 28px', flexDirection: 'row', alignItems: 'center', gap: 28 }}>
      <Text style={{ fontSize: 56, fontWeight: 'bold', color: '#F59E0B', lineHeight: 1 }}>0</Text>
      <Text style={{ fontSize: 18, color: '#FFFFFF', lineHeight: 1.6 }}>阶段内容文件<span style={{ fontWeight: 'bold' }}>一个都不填</span>也能开课 —— 缺失即有降级兜底；把 stages/ 填上后质量自然提升，<span style={{ fontWeight: 'bold' }}>不需要改任何代码</span>。</Text>
    </Box>
    <Box style={{ flex: 1, flexDirection: 'row', gap: 22 }}>
      <Box style={{ flex: 1, background: '#E8F0FB', borderRadius: 12, padding: '18px 22px', gap: 10 }}>
        <Text style={{ fontSize: 18, fontWeight: 'bold', color: '#0F2E5C' }}>缺什么 → 怎么降级</Text>
        <Box style={{ gap: 9 }}>
          {[
            ['questions.md 空', '改用 TMISSION 检验问题 → KNOWLEDGE-BASE 检测问题'],
            ['rubric.md 空', '只用 MASTERY-STAR-RULES.md 通用判星标准'],
            ['prompt.md 空', '用内置默认提示词（如「用自己的话讲回来」）'],
            ['讨论阶段无内容', '提示「请老师主导」+ 按 minutes 计时，到点切幕'],
          ].map((r, i) => (
            <Box key={i} style={{ flexDirection: 'row', gap: 12 }}>
              <Text style={{ fontSize: 13, fontWeight: 'bold', color: '#1B4F9C', width: 128 }}>{r[0]}</Text>
              <Text style={{ fontSize: 13, color: '#0F2E5C', flex: 1, lineHeight: 1.55 }}>{r[1]}</Text>
            </Box>
          ))}
        </Box>
      </Box>
      <Box style={{ flex: 1, border: '1px solid #D7E4F6', borderRadius: 12, padding: '18px 22px', gap: 10 }}>
        <Text style={{ fontSize: 18, fontWeight: 'bold', color: '#0F2E5C' }}>开课前 8 项校验（load_plan 执行）</Text>
        <Text style={{ fontSize: 13, color: '#0F2E5C', lineHeight: 1.85 }}>
          ① lesson-plan.json 存在且合法　② total_minutes &gt; 0<br />
          ③ 启用阶段分钟之和 ≤ 总时长　④ segments 文件齐全<br />
          ⑤ 复述/探究/讨论幕的 stages/&lt;id&gt;/ 目录存在<br />
          ⑥ runtime/ 模板齐全（缺失自动生成空白模板）<br />
          ⑦ KNOWLEDGE-BASE.md 存在（缺失仅警告，可开课不能提问）<br />
          ⑧ advance_policy 字段齐全、取值合法
        </Text>
        <Text style={{ fontSize: 12.5, color: '#64748B', lineHeight: 1.6, marginTop: 'auto' }}>校验只到「目录/文件存在」—— 文件是空的也算通过，空壳是合法状态。硬项失败则拒绝开课并写入 DIALOGUE-LOG。</Text>
      </Box>
    </Box>
  </Box>
  <Box style={{ position: 'absolute', left: 64, right: 64, bottom: 20, flexDirection: 'row', justifyContent: 'space-between' }}>
    <Box style={{ flexDirection: 'row', alignItems: 'center', gap: 8 }}>
      <Box style={{ width: 10, height: 10, background: '#1B4F9C', borderRadius: 2 }} />
      <Text style={{ fontSize: 13, color: '#64748B' }}>主动引导智能体 · 项目介绍</Text>
    </Box>
    <Text style={{ fontSize: 13, color: '#64748B' }}>09 / 18</Text>
  </Box>
</Slide>

- 生成《主动引导智能体介绍》PPT（18 页，A·科技深蓝风格）+ 同名 HTML 翻页版，位于 主动引导智能体介绍/。四章结构：学生端六幕流程与星级 / 老师课前配置（lesson-plan.json + 内容文件地图 + 空壳降级与8项校验）/ 编排器代码（11节点图、节点×文件对照、teach两层铁律、tick与切幕判定）/ 功能板块文件地图与分层装配。制作中用 slidep lint+upsert 逐页写入；坑：内容区容器必须显式 bottom 约束（如 bottom:56）否则 child-containment 溢出报错。

## GitHub 首次推送（2026-09-19）
- 本仓库已关联远端 origin = github.com/HELLO-APL/Dify-Classroom-Interactive-Agent（旧版项目仓库）。
- 策略：保留远端旧版 2 个提交，用 commit-tree 生成合并提交 9c1c57e（树=本地内容），追加在旧历史之上，非破坏。
- 本地分支已从 master 改名/切换为 main 并跟踪 origin/main。
- 坑：沙箱代理 502 拒 github.com:443（api.github.com 却通），git 网络操作必须 dangerouslyDisableSandbox + unset proxy 直连；且沙箱内 git update-ref/fetch 写 refs/remotes 不落盘，必要时直接写 .git/refs/remotes/origin/main 文件。
- 凭据走 Windows Credential Manager（git credential fill 可取，username=206093719），勿外泄。

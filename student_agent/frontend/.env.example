# 独立后端地址；不配置时请求当前站点的同源 /api。
# FastAPI 同源托管静态构建时保持为空；单独运行 Next.js 开发服务时填后端地址。
# 注意：NEXT_PUBLIC_* 是构建期变量，改了要重新 build 才生效。
# 跨域部署（前端与 API 不同源）时，还需在后端 .env.local 同步设置：
#   AGENT_CORS_ORIGIN_REGEX=<前端来源正则>   放行跨域请求
#   AGENT_COOKIE_SAMESITE=none + AGENT_COOKIE_SECURE=1   允许跨域携带登录 Cookie（要求 HTTPS）
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000

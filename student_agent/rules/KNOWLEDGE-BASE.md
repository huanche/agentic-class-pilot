# KNOWLEDGE-BASE

老师维护的稳定知识点目录。知识点编号一学期不变，AI 只读不写。以下为第3章样例，正式课程按学期维护。

> **2026-09 重构**：新增四个**探究字段**（`为什么这样设计` / `如何实现` / `解决什么实际问题` / `关联学科`）。
> 这四个字段是**深层探究阶段（70-85%）的唯一燃料**。不填也能跑（会降级），但填了探究才有方向。

---

## 字段说明

| 字段 | 必填 | 用在哪一幕 |
| --- | --- | --- |
| `定义` | 是 | 讲解阶段 |
| `检测问题` | 是 | 复述阶段的题库来源 |
| `掌握表现` | 是 | 判星级依据 |
| `为什么这样设计` | **推荐** | 深层探究 · 为什么方向 |
| `如何实现` | **推荐** | 深层探究 · 如何方向 |
| `解决什么实际问题` | **推荐** | 深层探究 · 用在哪方向 |
| `关联学科` | 可选 | 深层探究 · 跨学科方向 |

---

## KP-001 调度是什么

- 定义: CPU 一次只能服务一个进程，系统决定多个就绪进程先服务谁的规则就是处理机调度。
- 检测问题: 为什么系统需要调度，而不是让每个进程一直运行到结束？
- 掌握表现: 能说出 CPU 同一时刻只能运行一个进程，系统需要决定谁先运行、运行多久、何时切换。
- 为什么这样设计: 
- 如何实现: 
- 解决什么实际问题: 
- 关联学科: 

## KP-002 三级调度

- 定义: 高级调度调度作业进入内存并创建进程；中级调度负责对换；低级调度从就绪进程中选择进程上 CPU。
- 检测问题: 外存中的作业变成内存中的就绪进程，分别由哪一级调度完成？
- 掌握表现: 能区分三级调度的对象和时机，不会把作业调度与进程调度混为一谈。
- 为什么这样设计: 
- 如何实现: 
- 解决什么实际问题: 
- 关联学科: 

## KP-003 调度评价指标

- 定义: 周转时间、带权周转时间、平均等待时间、响应时间是评价调度算法的指标。
- 检测问题: 周转时间、等待时间和响应时间分别从哪个时刻算到哪个时刻？
- 掌握表现: 能正确区分三者的含义并完成计算。
- 为什么这样设计: 
- 如何实现: 
- 解决什么实际问题: 
- 关联学科: 

## KP-004 FCFS 与 SJF

- 定义: FCFS 按到达顺序执行；SJF 按运行时间短的先执行，平均等待时间更短但长作业可能饥饿。
- 检测问题: 为什么 SJF 的平均等待时间通常更短，却可能让长作业饥饿？
- 掌握表现: 能解释 SJF 的优势与饥饿来源，并能画出甘特图计算平均等待时间。
- 为什么这样设计: 
- 如何实现: 
- 解决什么实际问题: 
- 关联学科: 

## KP-005 抢占式与非抢占式

- 定义: 非抢占式不打断正在运行的进程；抢占式可以因新到达进程更短、时间片耗尽或优先级更高而中断当前进程。
- 检测问题: 抢占式 SJF 下，进程 A 正在运行，运行时间更短的 B 到达，系统会怎么做？
- 掌握表现: 能判断一个调度算法是否允许打断当前进程，并能用抢占式 SJF 的例题说明。
- 为什么这样设计: 
- 如何实现: 
- 解决什么实际问题: 
- 关联学科: 

## KP-006 时间片轮转与多级反馈队列

- 定义: RR 用时间片轮流服务进程，响应更快；多级反馈队列允许进程在不同队列间移动，不需要预先知道进程长度。
- 检测问题: 多级队列与多级反馈队列最大的区别是什么？
- 掌握表现: 能说清 RR 与 SJF 的取舍，以及多级反馈队列为什么能避免预先估计进程长度。
- 为什么这样设计: 
- 如何实现: 
- 解决什么实际问题: 
- 关联学科: 

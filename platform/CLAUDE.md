# 项目级规则

## 文档存放

- 生成的文档（计划、盘点、迁移方案等）不要放在 Claude 自己的目录（如 `C:\Users\<user>\.claude\...`），要放在项目内的 `docs/` 文件夹。
- 命名跟随现有惯例：英文 kebab-case，如 `docs/model-factory-migration-plan.md`、`docs/langgraph-migration-plan.md`。

"""LangGraph agent package (migrating from the vendor template, step by step).

Step 1 landed the message utilities (``utils.py``); step 2 added the tool
registry (``tools/``) and the agent itself (``graph.py``) with Postgres
checkpointing. See docs/langgraph-migration-plan.md.
"""
